import UIKit

enum DogImageCellConstants {
    static let identifier = "CustomImageCell"
    static let empty = ""
    enum CellMargins {
        static let top = CGFloat(0)
        static let bottom = CGFloat(0)
        static let trailing = CGFloat(-10)
        static let leading = CGFloat(10)
        
    }
    
    enum Texts {
        static let title = "nombre del perrito"
        static let placeholder = "perrito.jpg"
    }
}
