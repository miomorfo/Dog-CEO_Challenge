import UIKit

enum DogInfoViewConstants {
    static let title = "Vista Detalle"
    static let empty = ""
    
    enum CellMargins {
        static let top = CGFloat(10)
        static let bottom = CGFloat(-10)
        static let trailing = CGFloat(-10)
        static let leading = CGFloat(10)
        
    }
    
    enum AlertTexts {
        static let title = "Alerta"
        static let message = "EL perro alerta"
        static let actionOK = "OK"
    }
}
